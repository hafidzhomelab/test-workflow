version: '3.8'

services:
  web:
    image: nginx:alpine
    container_name: test-workflow-staging
    ports:
      - "${PORT:-8090}:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
    restart: unless-stopped
